# Решения — хронологический журнал

> Короткий журнал ключевых решений: дата, что решили, причина. Записи не переписываются — новые решения добавляются новыми записями. Формальные архитектурные решения — отдельно, в docs/adr/ (формат — docs/adr/ADR-000-template.md).

## Правила

- Запись: `## ГГГГ-ММ-ДД` + пункты `- **Решение.** Причина: ...`
- Новые записи добавляются сверху.
- Уже написанные записи не редактируются — при изменении решения пишется новая запись.
- Решения, меняющие инварианты или устройство проекта, оформляются как ADR в docs/adr/.

## 2026-09-01

- **Подэтап 9.4 „medium“ завершён (02.09.2026): исправлены ERR-10/20/21/23/24/25/13/14/26/32, ERR-06 закрыт решением владельца; ERR-27/28/30 отложены решением владельца; ERR-31 частично (anti-features/graveyard ждут владельца).** Причина: все medium-пункты либо исправлены и проверены на живом сайте, либо явно отложены владельцем; из 35 записей журнала открытыми остаются 4 отложенных.

- **ERR-06 (заглушки на /bezopasnost/): не трогать.** Причина: решение владельца — 12 блоков «Документ будет размещён» остаются.
- **Телефон питания +7 (8142) 78-00-33: пока не трогаем.** Причина: решение владельца; пометка остаётся в открытых проблемах HANDOFF.
- **ERR-14: кнопку «Версия для слабовидящих» оставляем, доки обновляем (режим — дополнение сверх требований).** Причина: решение владельца.
- **ERR-21: создаём страницу «Противодействие коррупции».** Причина: решение владельца (раздел из requirements получает страницу с честной пометкой о материалах).
- **ERR-26: AGENTS.md переписан под фактический git-процесс (коммиты в dev, main — резерв).** Причина: решение владельца.
- **ERR-30 (инварианты): отложено.** Причина: решение владельца; invariants.md остаётся шаблоном, пункт открыт в журнале.

- **Подэтап 9.3 „high“ завершён (01.09.2026): исправлены ERR-05/07/08/12/19/22/29/35; поиск работает на Pagefind, в CI — astro check и link-check.** Причина: все high-пункты, кроме ожидающих решения владельца (ERR-06), закрыты и проверены на живом сайте.

- **ERR-12: поиск делаем на Pagefind (статический поиск без сервера).** Причина: решение владельца; мёртвую форму заменяем рабочим поиском по всем страницам.
- **Дизайн-вопросы (включая топбар на ≤420px) — через OpenDesign; точечные CSS-фиксы ошибок допустимы в коде.** Причина: владелец напомнил инвариант „дизайн правится через OpenDesign“; текущее состояние топбара (e-mail скрыт на ≤420px) остаётся до дизайн-прохода.
- **Телефон горячей линии по питанию +7 (8142) 78-00-33 — пока не трогаем.** Причина: владелец не ответил на вопрос; пометка в открытых проблемах HANDOFF.
- **ERR-06 (12 заглушек на /bezopasnost/) — отложен без решения.** Причина: владелец не ответил на вопрос; остаётся открытым в журнале.

- **Подэтап 9.2 „мобильный проход“ завершён (01.09.2026): ERR-34/16/17/18 исправлены и проверены на живом сайте (шапка 160px, hero 592px, контент на Y≈990px, футер 759px, sticky-ФИО, тач-таргеты ≥24px, сайдбар-лента; все 52 страницы на 360px без разлива); найден новый ERR-35 (режим „для слабовидящих“ разливает на мобиле).** Причина: этап 9.2 выполнен по плану, десктоп не затронут (проверено побайтово), найдена смежная ошибка — заведена в журнал.
- **Подэтап 9.1 «критические фиксы» завершён (01.09.2026): исправлены ERR-01/02/03/04/09/11/15, ERR-33 закрыт решением владельца; все 52 страницы проверены на 360px — горизонтального разлива нет.** Причина: решения 9.0 получены, фиксы закоммичены в dev (93bbfd0…99edbed) и верифицированы на живом сайте; критерий ERR-15 достигнут скриптовым прогоном всех страниц.
- **ERR-01 контакты: единый набор с Nubex (+7 (8142) 33-20-29 / sch29-ptz@yandex.ru / Пн–Пт 08:30–16:45, Сб 08:30–14:00).** Причина: владелец не знает, какой телефон реальный; данные с официального старого сайта школы — самый надёжный доступный источник. Остаётся пометка «подтвердить у школы» (docs/errors.md ERR-01).
- **ERR-03 документы: 6 федеральных/муниципальных документов заменить официальными гос. ссылками, остальные ~100 школьных — оставить названиями без URL с пометкой «файлы будут размещены при запуске».** Причина: законы и акты мэрии публикуются на официальных порталах; файлы самой школы качать не будем (репо не толстеет, деплой не замедляется).
- **ERR-11 форма обращений: убрать мёртвую форму, оставить ссылки на Госуслуги, почту и личный приём.** Причина: форма с action="#" теряет сообщения; честная альтернатива — официальные каналы.
- **ERR-33: дисклеймер «неофициальный сайт» НЕ добавляем; сайт остаётся public на GitHub Pages.** Причина: решение владельца — учебный проект, продвигать его никуда не планируется. Риск (возможность принять сайт за официальный) владельцем осознан и принят.
- **Этап 9: все 5 решений подэтапа 9.0 приняты владельцем 01.09.2026, блокировки сняты — 9.1 стартует.** Причина: владелец ответил на все вопросы 9.0 (контакты, документы, форма, дисклеймер, статус публикации).
- **Создан docs/errors.md — журнал всех ошибок сайта (34 записи: факт с доказательством, влияние, исправление, приоритет).** Причина: полный аудит проекта 01.09.2026 (код, git, живой сайт, мобильная проверка на 360px) выявил ошибки от противоречащих контактов до мобильного разлива; владелец запросил файл «все ошибки + исправления + приоритеты».
- **Решено: мобильную версию полностью НЕ переделывать — вместо этого точечный «мобильный проход».** Причина: адаптивный скелет работает (бургер-меню с focus-trap, 5 брейкпоинтов, карточки в одну колонку, «версия для слабовидящих» с реальными стилями); поломки локальны (горизонтальный разлив — одна строка CSS, вертикальная раздутость, тач-таргеты, сайдбар). Порядок: сначала топ-5 из docs/errors.md, затем мобильный проход по фиксированному списку (ERR-15, ERR-16, ERR-17, ERR-18, ERR-34) с прогоном всех 52 страниц на 360px. Если после прохода мобильная версия останется неприемлемой — вернуться к вопросу о переделке.
- **Этап 8 «навигация и ссылки» завершён (01.09.2026): аудит ссылок — docs/research/broken-links-2026-09-01.md: найдено 78 битых URL (72 файла Nubex /_/static/r1.nubex.ru/..., 5 файлов /food/*.xlsx, 1 внешняя sch29.nubex.ru) и 61 уникальная заглушка href="#" (1166 вхождений) → 8a (коммит d6bf314: страницы разделов — /parents/ (приём в 1/10 класс, FAQ, Малышок), /students/ (одарённые дети, спортклуб, СПТ, отдых детей), /teachers/ (нагрузка), общие /gia/ и /nastavnichestvo/ (для двух меню); контент с Nubex; все ссылки меню работают), 8b (коммит a20e01d: служебные страницы — /privacy/ (Политика ПД, заготовка по 152-ФЗ, требует утверждения), /sitemap/, /links/ (10 проверенных гос. ресурсов); футер — реальные ссылки, соцсети-заглушки убраны (реального госпаблика нет); виджеты главной — реальные URL (pos.gosuslugi.ru, myschool.edu.ru, bus.gov.ru/qrcode/rate/24091, госуслуги «запись в 1 класс»); убраны 77 битых markdown-ссылок на файлы), 8c (коммит 9be30c4: фильтры новостей — 6 статических страниц /news/tag/<slug>/ (транслит рубрик, NewsFilters.astro), пагинация-заглушка удалена; документы на /bezopasnost/ — заглушки убраны («Документ будет размещён»); href="#" в проекте — 0).** Причина: критерий этапа 8 достигнут — на сайте нет ни одной битой ссылки и ни одной ссылки-заглушки, каждый пункт меню ведёт на реальную страницу; созданы служебные страницы (политика ПД, карта сайта, ссылки на гос. ресурсы); открытые проблемы остаются: реальные URL соцсетей/госпаблика (владелец), файлы документов (6d), проверка контента владельцем, слаги-кириллица, ИНН/КПП/ОГРН.

## 2026-08-31

- **Этап 7 «проверки и полировка» завершён (31.08.2026, по факту 01.09.2026): аудит опубликованного сайта (docs/research/audit-2026-08-31.md — Lighthouse 13.4.1, html-validate 11.11.0, W3C Nu Validator, Playwright, скрипт расчёта контраста WCAG) → исправления 7a (коммит 3038dbc «fix: удаление тестовых новостей, оптимизация шрифтов, a11y-правки (этап 7a)»: удалены тестовые новости «Тест» и «Тестовая новость», шрифты 24 → 10 woff2, ~95 КБ вместо ~100 КБ, a11y-правки) и 7b (коммит 55210c7 «fix: SEO-мета (canonical/OG), короткие title статей, focus-trap меню (этап 7b)»: canonical/OG-мета в Layout.astro, site в astro.config.mjs, title статей ≤ 70 символов, focus-trap мобильного меню в main.js). Финальный Lighthouse на живом сайте: Perf 99–100, A11y 100, BP 100, SEO 100. Все этапы 1–7 завершены, сайт готов: https://ntiwgg.github.io/school29-site/.** Причина: критерий этапа 7 (доступность, скорость, валидность) достигнут — сайт на живом деплое проходит проверки с максимальными баллами, тестовый контент из прода удалён, найденные аудитом замечания (SEO-мета, длина title, focus-trap) закрыты.
- **6d «перенос файлов документов с Nubex» пропущен (решение владельца, 31.08.2026): файлы документов не переносятся; перечень 106 документов виден на /sveden/dokumenty/ из markdown-тела dokumenty.md; ссылки вида /_/static/r1.nubex.ru/... остаются нерабочими — перенести файлы при реальном запуске. Зафиксировано в docs/tech-debt.md и в открытых проблемах docs/HANDOFF.md.** Причина: сайт не запускается в production (учебно-портфельный проект), перенос 106 файлов не оправдан на текущей стадии; при реальном запуске файлы будут перенесены и блок «Прикреплённые файлы» из коллекции documents наполнится.
- **Перенос контента с Nubex (этапы 6a–6c завершены 31.08.2026): реальные данные перенесены со старого сайта в коллекции — контакты (src/content/contacts.md: адрес наб. Варкауса, 5; +7 (8142) 33-20-29; sch29-ptz@yandex.ru; режим работы), основные сведения, руководство (коммит 2e25dc0 «feat: реальные контакты, основные сведения, руководство с Nubex (этап 6a)»); педсостав — 75 педагогов вместо 13 примеров, статистика на /sveden-teachers/ посчитана из коллекции (75: 11 — высшая категория, 8 — первая, 56 — без категории), плейсхолдеры контактов заменены (коммит a11cba9 «feat: реальный педсостав с Nubex + фикс плейсхолдеров контактов (этап 6b)»); тексты 14 подразделов «Сведений» — реальные с Nubex вместо плейсхолдеров (коммиты 6601861 «структура, документы, образование, МТО» и c6cbf62 «платные услуги, ФХД, вакансии, стипендии, международное сотрудничество, питание, стандарты», этап 6c-1/6c-2). Решение по «Документам»: основной контент /sveden/dokumenty/ — markdown-тело dokumenty.md с реальным перечнем из 106 документов; коллекция documents подключена отдельным блоком «Прикреплённые файлы» под перечнем (в коде [slug].astro — attachedDocs, getCollection('documents') с фильтром по полю file), блок рендерится только если появились записи с загруженными файлами.** Причина: подэтапы 6a–6c завершены — разделы «Сведений», контакты и педсостав наполнены реальным актуальным контентом со старого сайта (проверка актуальности владельцем — docs/content-verification.md); перечень документов (106 позиций) уже лежит в markdown-теле — дублировать его записями коллекции не нужно, а коллекция documents остаётся для прикреплённых файлов (заполнится на этапе 6d, когда файлы будут перенесены).
- **CI на GitHub Pages: любой push в dev (включая коммиты из Decap-админки) → сборка → деплой на https://ntiwgg.github.io/school29-site/; base '/school29-site/' в astro.config.mjs; редиректы /admin удалены (на статике не работают, /admin/ отдаёт public/admin/index.html — закрыт tech-debt о коллизии роутов); хелпер src/lib/paths.ts url(path) для BASE_URL (Astro не переписывает корневые ссылки — все внутренние ссылки и ссылки на public-ассеты в 12 .astro-файлах обёрнуты в url()/BASE_URL); репозиторий ntiwgg/school29-site переведён в public (одобрено владельцем)** (коммит 8627b20 «ci: GitHub Pages деплой + base path (этап 5c)» на ветке dev; workflow success 25s). Причина: подэтап 5c завершён — правки из админки Decap автоматически публикуются на живой сайт (~30 сек на деплой), все ключевые страницы доступны по публичному URL (главная, /news/, /sveden/, /admin/, /assets/css/styles.css — 200); base нужен, потому что сайт живёт в подпути /school29-site/; Astro-редиректы на статическом GitHub Pages не работают, а /admin/ и так отдаёт public/admin/index.html; корневые ссылки в разметке Astro не переписывает — префикс добавляется хелпером paths.ts.
- **Все коллекции контента готовы: news, announcements, sveden (14 подразделов), teachers (13 педагогов), documents (4 документа), contacts (единый файл src/content/contacts.md); Decap подключён ко всем шести коллекциям** (коммиты 399f6ab — коллекция teachers, e41254c — коллекция contacts). Причина: подэтапы 5b-2.3 «Педагогический состав» (коллекция teachers, 13 педагогов 1:1 из разметки; поля по ПП РФ № 1802: name, position, subjects, category, education, degree, profdev, retraining, experience, programs; order — порядок строк; таблица на /sveden-teachers/ рендерится из коллекции) и 5b-2.4 «Документы» и «Контакты» (коллекция documents — 4 примера с полями title/doc-type/date/file/description, список на /sveden/dokumenty/ рендерится из коллекции, стили .docs-list; коллекция contacts — single-file, getEntry, поля address/phones/email/workhours/requisites) завершены — весь контент сайта наполняется через админку. Плейсхолдеры контактов (адрес ул. Лыжная д. 4, ИНН/ОГРН) — выдуманные дизайном данные, заменить реальными на этапе 6.
- **Коллекция «Сведения об образовательной организации»: 14 подразделов по приказу Рособрнадзора № 1493 в src/content/sveden/*.md (поля num/title/description, тело — markdown-плейсхолдер «Раздел готовится к наполнению»); страницы /sveden/<slug>/ с хлебными крошками и сайдбаром из 14 подразделов; /sveden/ рендерит карточки из коллекции (SvedenCard.astro, сортировка по num); «Педагогический состав» ведёт на отдельную страницу /sveden-teachers/ — /sveden/pedagogicheskiy-sostav/ не генерируется (404); Decap коллекция sveden добавлена в config.yml** (коммит 242eb07 на ветке dev; build — 31 страница). Причина: подэтап 5b-2.2 завершён — подразделы «Сведений об ОО» редактируются через админку, структура соответствует приказу № 1493.
- **Фильтр планирования публикации: записи news и announcements с датой в будущем скрываются с сайта (лента, главная, страница будущей записи → 404), но остаются в git и админке. Реализация: isPublished() в src/lib/news.ts (сравнение по началу дня, без сдвигов часовых поясов), применена в index.astro (главная: новости и объявления), news/index.astro (лента) и news/[slug].astro (фильтр + 404); в Decap config.yml добавлены hint к полю date обеих коллекций** (коммит b286fb2 на ветке dev). Причина: секретарь/учитель может подготовить запись заранее — она появится на сайте в нужный день, без ручных действий.
- **Фикс путей Decap подтверждён владельцем: тестовая новость «Тестовая новость» создана через админку Decap (коммит 7ec1117 от Decap в ветке dev), легла в site/src/content/news/2026-08-30-тестовая-новость.md и отображается на сайте — конвейер «админка → GitHub → dev → сайт» работает полностью** (подтверждение фикса коммита ebf6d25). Причина: префикс site/ в путях config.yml устранил баг, при котором контент из админки не попадал на сайт.
- **Создана коллекция «Объявления»: 4 записи в src/content/announcements/, поле important, иконки через компонент AnnouncementIcon.astro; главная рендерит объявления из коллекции; Decap config.yml дополнен коллекцией announcements** (коммит 8e36db5 на ветке dev). Причина: подэтап 5b-2.1 завершён — блок объявлений на главной наполняется через админку.
- **Исправлен критический баг путей Decap: Decap резолвит folder/media_folder от корня git-репозитория, а не от site/, поэтому тестовая новость из админки создавалась в корне (src/content/news/) и не попадала на сайт. Все пути в site/public/admin/config.yml получили префикс site/ (folder: site/src/content/news, site/src/content/announcements, media_folder: site/public/uploads); тестовая новость перенесена git mv в site/src/content/news/2026-08-30-тест.md; в config.yml добавлен комментарий-инвариант** (коммит ebf6d25 на ветке dev). Причина: без префикса контент из админки не попадал на сайт. Подтверждение фикса ждём от владельца (новая тестовая новость через админку).
- **Транслитерация слагов в Decap невозможна** — проверено по бандлу: карты кириллицы нет, slug.encoding unicode/ascii — кириллица остаётся или заменяется дефисами; слаги остаются кириллическими, «пока работает» (кириллические URL рабочие; см. docs/tech-debt.md). Причина: URL с кириллицей нежелательны, но средствами Decap не решаются.
- **Decap CMS проверен владельцем: вход через «Login with GitHub» работает (дефолтный OAuth Decap), тестовая новость «Тест» создана через админку** (коммит d095eb4c «Create Новость "2026-08-30-тест"» в ветке dev, файл site/src/content/news/2026-08-30-тест.md, frontmatter корректный). Причина: подэтап 5b-1 полностью завершён — редактирование через веб-интерфейс подтверждено владельцем. Замечание: Decap создаёт слаги с кириллицей (имя файла «2026-08-30-тест.md») — настроить транслитерацию слагов в config.yml (см. docs/tech-debt.md).
- **GitHub-репозиторий ntiwgg/school29-site (private) с ветками main и dev (remote origin по SSH); Decap CMS подключён (CDN decap-cms@^3.16.0, вход через дефолтный GitHub OAuth, публикация правок в ветку dev); коллекция news с 9 записями (Astro content collections, src/content.config.ts); динамические страницы новостей /news/ и /news/[slug]/** (коммит 4a2424f на ветке dev). Причина: подэтап 5b-1 завершён — новости редактируются через веб-интерфейс и публикуются в git; репозиторий нужен для версионирования и публикации правок Decap.
- **Этап 4 завершён: Astro-проект в site/, дизайн перенесён 1:1** (ветка dev, коммит dc76d39). Решение: `compressHTML: false` для идентичности вёрстки. Причина: сохраняем разметку 1:1 с HTML-дизайном (в т.ч. пробелы между inline-элементами), чтобы не менять визуал; минификацию можно вернуть позже.
- **Утверждена информационная архитектура** — главное меню из 9 пунктов; спецразделы («Опорная школа РЖД», «Школа Малышок») упакованы в структуру; виджеты Госуслуг («Решаем вместе», ссылка на bus.gov.ru) оставлены. Причина: этап 2 завершён — структура разделов и навигация утверждены владельцем 31.08.2026. Детали — docs/requirements.md, раздел «Информационная архитектура».
- **Выбран стек Astro + Decap CMS.** Причина: современный статический генератор — быстро, доступно, легко проверяется; Decap CMS — git-based веб-редактор, чтобы контент мог наполнять нетехнический человек (секретарь/учитель).
- **Домен пока не нужен.** Причина: запуск в production не планируется.
- **Запуск в production не планируется.** Причина: проект учебно-портфельный («по приколу, но полностью реальный и серьёзный»); при этом делается по всем современным требованиям, как реальный.
- **Дизайн будет создаваться через OpenDesign.** Причина: дизайн — отдельный инструмент и рабочий процесс, не делается руками в коде.
- **Контент будет наполнять секретарь/учитель (нетехнический человек)** — если проект когда-нибудь запустится. Причина: в школе нет выделенного веб-редактора; наполнение должно быть доступно без знания кода.
- **Работа ведётся строго поэтапно, маленькими шагами.** Причина: проект портфельный; этапы с проверяемыми результатами держат фокус и не дают размыть объём.
