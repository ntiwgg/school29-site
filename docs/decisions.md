# Решения — хронологический журнал

> Короткий журнал ключевых решений: дата, что решили, причина. Записи не переписываются — новые решения добавляются новыми записями. Формальные архитектурные решения — отдельно, в docs/adr/ (формат — docs/adr/ADR-000-template.md).

## Правила

- Запись: `## ГГГГ-ММ-ДД` + пункты `- **Решение.** Причина: ...`
- Новые записи добавляются сверху.
- Уже написанные записи не редактируются — при изменении решения пишется новая запись.
- Решения, меняющие инварианты или устройство проекта, оформляются как ADR в docs/adr/.

## 2026-08-31

- **Этап 4 завершён: Astro-проект в site/, дизайн перенесён 1:1** (ветка dev, коммит dc76d39). Решение: `compressHTML: false` для идентичности вёрстки. Причина: сохраняем разметку 1:1 с HTML-дизайном (в т.ч. пробелы между inline-элементами), чтобы не менять визуал; минификацию можно вернуть позже.
- **Утверждена информационная архитектура** — главное меню из 8 пунктов; спецразделы («Опорная школа РЖД», «Школа Малышок») упакованы в структуру; виджеты Госуслуг («Решаем вместе», ссылка на bus.gov.ru) оставлены. Причина: этап 2 завершён — структура разделов и навигация утверждены владельцем 31.08.2026. Детали — docs/requirements.md, раздел «Информационная архитектура».
- **Выбран стек Astro + Decap CMS.** Причина: современный статический генератор — быстро, доступно, легко проверяется; Decap CMS — git-based веб-редактор, чтобы контент мог наполнять нетехнический человек (секретарь/учитель).
- **Домен пока не нужен.** Причина: запуск в production не планируется.
- **Запуск в production не планируется.** Причина: проект учебно-портфельный («по приколу, но полностью реальный и серьёзный»); при этом делается по всем современным требованиям, как реальный.
- **Дизайн будет создаваться через OpenDesign.** Причина: дизайн — отдельный инструмент и рабочий процесс, не делается руками в коде.
- **Контент будет наполнять секретарь/учитель (нетехнический человек)** — если проект когда-нибудь запустится. Причина: в школе нет выделенного веб-редактора; наполнение должно быть доступно без знания кода.
- **Работа ведётся строго поэтапно, маленькими шагами.** Причина: проект портфельный; этапы с проверяемыми результатами держат фокус и не дают размыть объём.
